export function toUpper(str) {
  return str.toUpperCase()
}

export function toLower(str) {
  return str.toLowerCase();
}

export function len(str) {
  return str.length;
}

// console.log("toUpper", toUpper("Cтрока"), "toLower", toLower("Cтрока"), "len", len("Cтрока"))